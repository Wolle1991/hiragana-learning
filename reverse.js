import {shuffle,pick,filterByDifficulty} from '../core/utils.js';export function createReverseRound(items,difficulty='easy'){const pool=filterByDifficulty(items,difficulty);const current=pick(pool);const wrong=shuffle(pool.filter(i=>i.char!==current.char)).slice(0,3).map(i=>i.char);return{current,options:shuffle([current.char,...wrong]),prompt:'Welches Zeichen passt zu diesem Romaji?'}}