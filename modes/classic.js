import {shuffle,pick,filterByDifficulty} from '../core/utils.js';export function createClassicRound(items,difficulty='easy'){const pool=filterByDifficulty(items,difficulty);const current=pick(pool);const wrong=shuffle(pool.filter(i=>i.romaji!==current.romaji)).slice(0,3).map(i=>i.romaji);return{current,options:shuffle([current.romaji,...wrong]),prompt:'Welches Romaji passt zu diesem Zeichen?'}}
