export const shuffle=(arr)=>[...arr].sort(()=>Math.random()-0.5);export const pick=(arr)=>arr[Math.floor(Math.random()*arr.length)];export const uidDate=()=>new Date().toISOString().slice(0,10);export function difficultyRank(level){return{easy:1,medium:2,hard:3}[level]??2}export function filterByDifficulty(items,level){const max=difficultyRank(level);return items.filter(i=>difficultyRank(i.difficulty??'medium')<=max)}
