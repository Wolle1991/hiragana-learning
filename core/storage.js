const KEY='kanaTrainerStateV1';export function defaultState(){return{settings:{theme:'dark'},progress:{hiragana:{correct:0,wrong:0,streak:0,bestStreak:0,learned:[],mistakes:{}},katakana:{correct:0,wrong:0,streak:0,bestStreak:0,learned:[],mistakes:{}}},daily:{},lastSelection:{script:'hiragana',difficulty:'easy',row:'all',writeDir:'kana-to-romaji'}}}export function loadState(){try{return{...defaultState(),...(JSON.parse(localStorage.getItem(KEY))||{})}}catch{return defaultState()}}export function saveState(state){localStorage.setItem(KEY,JSON.stringify(state))}
